The ``1-my_list`` module
======================

Using ``MyList``
-------------------

This is an 1-my_list text file in reStructuredText format.  First import
``MyList`` from the ``1-my_list`` module:

    >>> MyList = __import__('1-my_list').MyList
    >>> my_list = MyList()

Now use it:

    >>> my_list.print_sorted(6, 4)
    10
    >>> my_list.print_sorted(1)
    99
    >>> my_list.print_sorted('u')# doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    TypeError: a must be an integer
    >>> my_list.print_sorted(1, 'u')# doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    TypeError: b must be an integer
    >>> my_list.print_sorted(1, 1.2)
    2
    >>> my_list.print_sorted(1.3, 2)
    3
    >>> my_list.print_sorted(1.3, 2.6)
    3
    >>> my_list.print_sorted(1.3, "2")# doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    TypeError: b must be an integer
    >>> my_list.print_sorted("1.3", 2)# doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    TypeError: a must be an integer
    >>> my_list.print_sorted()# doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    TypeError: a must be an integer
    >>> a = float("inf")
    >>> b = 10
    >>> my_list.print_sorted(a, b)  # doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    OverflowError: int too large to convert to float
    >>> my_list.print_sorted(float('nan'), 2)  # doctest: +IGNORE_EXCEPTION_DETAIL
    Traceback (most recent call last):
        ...
    ValueError: cannot convert float NaN to integer
